# Documentation

This directory contains active documentation for the synthetic public demo.

## Active

- [Architecture](architecture.md): workflow map from synthetic prices to summary and
  `targets.json`.

## Archive

- [Archived provenance](../archive/README.md): public-safe historical notes that are
  preserved only as frozen context.

The active workflow uses synthetic fixtures only. It has no dependency on the
private workspace, licensed data, provider adapters, broker integration, or
shared data roots.
