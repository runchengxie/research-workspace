[project]
name = "hk-cross-sectional-strategy-demo"
version = "0.1.0"
description = "Paused-maintenance synthetic HK cross-sectional strategy demo"
requires-python = ">=3.12"

[tool.unittest]
start-directory = "tests"
