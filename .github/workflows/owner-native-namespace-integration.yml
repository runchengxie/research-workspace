name: superseded owner-native namespace integration

on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by namespace-integration-debug.yml'
