name: retired namespace integration debug

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Root cause captured; superseded by owner-native-namespace-integration-v2.yml'
